version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11  # or your app’s version
    commands:
      - pip install -r requirements.txt

  build:
    commands:
      - echo "Zipping application..."
      - zip -r app.zip . -x "*.git*" "*.vscode*"

artifacts:
  files:
    - app.zip
